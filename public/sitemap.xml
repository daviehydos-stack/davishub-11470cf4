// api/sitemap.xml.ts
// Vercel Edge Function - Dynamically generates sitemap from Supabase
// Deploy to: /api/sitemap.xml.ts
// Access at: https://www.azaniispproject.co.ke/sitemap.xml (via vercel.json redirect)

import { createClient } from '@supabase/supabase-js';

const BASE_URL = 'https://www.azaniispproject.co.ke';

export const config = {
  runtime: 'edge',
};

export default async function handler(req: Request) {
  try {
    const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!;
    const supabaseKey = process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!;

    const supabase = createClient(supabaseUrl, supabaseKey);
    const today = new Date().toISOString().split('T')[0];

    // Fetch ALL blog posts and articles from Supabase
    const [blogPostsResult, articlesResult] = await Promise.all([
      supabase
        .from('blog_posts')
        .select('slug, updated_at, created_at')
        .eq('is_published', true)
        .order('created_at', { ascending: false }),
      supabase
        .from('generated_articles')
        .select('slug, created_at')
        .eq('is_published', true)
        .order('created_at', { ascending: false }),
    ]);

    const blogPosts = blogPostsResult.data || [];
    const articles = articlesResult.data || [];

    // Static pages with priorities
    const staticPages = [
      { path: '/', priority: 1.0, changefreq: 'daily' },
      { path: '/pricing', priority: 0.9, changefreq: 'weekly' },
      { path: '/kcse-guide', priority: 0.9, changefreq: 'weekly' },
      { path: '/blog', priority: 0.9, changefreq: 'daily' },
      { path: '/blogs', priority: 0.9, changefreq: 'daily' },
      { path: '/articles', priority: 0.9, changefreq: 'daily' },
      { path: '/resources', priority: 0.8, changefreq: 'weekly' },
      { path: '/past-projects', priority: 0.8, changefreq: 'monthly' },
      { path: '/video-tutorials', priority: 0.7, changefreq: 'weekly' },
      { path: '/faq', priority: 0.7, changefreq: 'monthly' },
      { path: '/contact', priority: 0.6, changefreq: 'monthly' },
      { path: '/about', priority: 0.5, changefreq: 'monthly' },
      { path: '/community', priority: 0.7, changefreq: 'daily' },
      { path: '/privacy-policy', priority: 0.3, changefreq: 'yearly' },
      { path: '/terms-of-service', priority: 0.3, changefreq: 'yearly' },
    ];

    // Generate XML sitemap
    let xml = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
`;

    // Add static pages
    staticPages.forEach(page => {
      xml += `
  <url>
    <loc>${BASE_URL}${page.path}</loc>
    <lastmod>${today}</lastmod>
    <changefreq>${page.changefreq}</changefreq>
    <priority>${page.priority}</priority>
  </url>`;
    });

    // Add dynamic blog posts
    blogPosts.forEach(post => {
      const lastmod = (post.updated_at || post.created_at || today).split('T')[0];
      xml += `
  <url>
    <loc>${BASE_URL}/blogs/${post.slug}</loc>
    <lastmod>${lastmod}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
  </url>`;
    });

    // Add dynamic articles (updated daily)
    articles.forEach(article => {
      const lastmod = (article.created_at || today).split('T')[0];
      xml += `
  <url>
    <loc>${BASE_URL}/articles/${article.slug}</loc>
    <lastmod>${lastmod}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>0.8</priority>
  </url>`;
    });

    xml += `
</urlset>`;

    return new Response(xml, {
      headers: {
        'Content-Type': 'application/xml; charset=utf-8',
        'Cache-Control': 'public, max-age=3600, s-maxage=3600, stale-while-revalidate=86400',
      },
    });

  } catch (error) {
    console.error('Sitemap error:', error);
    
    // Fallback sitemap
    const fallback = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>${BASE_URL}/</loc>
    <lastmod>${new Date().toISOString().split('T')[0]}</lastmod>
    <priority>1.0</priority>
  </url>
</urlset>`;

    return new Response(fallback, {
      headers: { 'Content-Type': 'application/xml', 'Cache-Control': 'public, max-age=300' },
    });
  }
}
